# Browser calls `/api`; Next proxies to Nest (see next.config.ts + app/api/[...path]).
NEXT_PUBLIC_API_URL=/api

# Nest origin (server-side only). Nest routes are under `/api/...`.
API_PROXY_TARGET=http://localhost:4000
